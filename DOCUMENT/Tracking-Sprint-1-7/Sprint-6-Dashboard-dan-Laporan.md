# Tracking Sprint 6 — Dashboard & Laporan

> **Pembaruan kanonis 25 Agustus 2026:** literal `status_aktif` dan pengecualian nama status pada catatan audit lama adalah snapshot historis dan **Superseded**. Dashboard/laporan aktif harus memakai predicate `ref_status_pegawai.kelompok` melalui scope kanonis; `Aktif/khusus` termasuk aktif. Lihat [Keputusan Lifecycle dan Status Pegawai](../Keputusan-Lifecycle-Status-Pegawai-25-Agustus-2026.md).

> **Addendum RBAC 2 September 2026:** status source Sprint 6 tidak membuktikan kontrak akses baru. `employees.export` kini diperlukan untuk raw export, sedangkan `reference_tables.manage` tidak boleh dikunci ke `super_admin` hanya karena konfigurasi lama. Penyebutan role gate dalam arsip di bawah adalah fakta implementasi saat itu dan memerlukan follow-up pada backlog RBAC, bukan pengubahan bukti historis.

| Field | Detail |
|---|---|
| Periode | 31 Juli – 9 Agustus 2026 |
| Cakupan issue | #39 – #43, #46 (`Issues-SIMPEG-Fase1.md`) |
| Pembaruan terakhir | 10 Agustus 2026 (rekonsiliasi final status source Sprint 6) |
| Basis verifikasi | Branch `development` setelah PR #170; status PR diverifikasi ulang 10 Agustus 2026 |
| Keputusan kickoff (kanonis) | `Kickoff-Sprint-6-Kontrak-dan-Keputusan.md` — **K-1** hapus reference table = hybrid `is_active` (tanpa SoftDeletes); **K-2** status "Dinas Luar" ditunda ke Fase 2; **K-3** kontrak payload Dashboard Admin W1–W7 |
| Pembagian tugas | Lihat pembagian BE/FE final 26 Juli 2026 (Jordan: #17/#21/#22-BE; Grantly: #16/#18/#19/#20/#23-BE + QA; Adithian & Adriel: sisi FE) |
| Menggantikan | `Analisis-Kesesuaian-Sprint-1-5.md` bagian Sprint 6 (dihapus 26 Juli 2026) |

Legend: ✅ selesai pada source · ⚠️ sebagian · ❌ belum selesai. Status source, bukan status tracker `Done`.

> **Penyelarasan kontrak Program Studi — 17 Agustus 2026:** keputusan baru menambahkan `ref_program_studi` sebagai reference table kesembilan dalam US-8.5. Penyebutan delapan reference table pada rekonsiliasi 10 Agustus dan arsip di bawah tetap dibaca sebagai cakupan sebelum keputusan ini. Kontrak Program Studi mengikuti [Keputusan Program Studi sebagai Data Referensi](../Keputusan-Program-Studi-Data-Master.md).

## Ringkasan

**6 ✅ · 0 ⚠️ · 0 ❌ pada source.** Seluruh issue Sprint 6 telah merge ke `development`: dashboard Admin/Pimpinan/Pegawai/Kepala Bagian, laporan Excel/PDF, dan delapan reference table. Simbol ini mengikuti legend status source; formal `Done`, full regression, dan UAT tetap menjadi release gate Sprint 7.

## Status Terkini per Issue — 10 Agustus 2026

| Issue | Deliverable | Status source | Bukti penutupan |
|---:|---|:---:|---|
| #39 | Dashboard Admin & Pimpinan (7 widget) | ✅ | PR #150 menghubungkan Dashboard Admin ke payload K-3, PR #158 memperkuat state data pada UI, dan PR #164 menghitung tren W7 dari riwayat pengangkatan. |
| #40 | Dashboard Pegawai (pribadi) | ✅ | PR #132 menyelesaikan Action/notifikasi dashboard; PR #167 menutup empty state saldo dan konsistensi tampilan. |
| #41 | Dashboard Kepala Bagian | ✅ | PR #125 menyelesaikan dashboard/quick action; PR #155 membatasi status bawahan Fase 1 ke aktif/cuti sesuai K-2. |
| #42 | Export daftar pegawai (Excel + PDF) | ✅ | PR #154 menyelesaikan export custom, PR #162 memperbaiki alur/filter laporan Admin, dan PR #167 menambahkan tombol Export PDF beserta penerusan filter dan test. |
| #43 | Export rekap cuti (Excel + PDF) | ✅ | PR #141 menyelesaikan query agregasi dan Excel; PR #167 menyelesaikan/menyeragamkan output PDF. |
| #46 | Kelola reference tables | ✅ | PR #151 menyelesaikan channel notifikasi, PR #165 menambah CRUD `ref_jabatan`, dan PR #170 menutup gap terakhir US-8.5 AC-3 dengan guard riwayat status serta FK `RESTRICT`. |

## Arsip Status per Issue — snapshot 29 Juli 2026

> Bagian ini dipertahankan sebagai kronologi kondisi sebelum PR penutup di atas. Jika ada pernyataan “sisa” atau “menunggu PR” di bawah, status terkini pada tabel 10 Agustus 2026 yang berlaku.

| Issue | Deliverable | Status | Bukti & catatan |
|---:|---|:---:|---|
| #39 | Dashboard Admin & Pimpinan (7 widget) | ⚠️ | **Pimpinan: real & diperbaiki PR #125** — W5 golongan granular I/a–IV/e, KPI `totalEwsAktif` terpisah dari 5 teratas, W2 golongan asal→tujuan, tren W7 metode baru (perbandingan historis `tanggal_pensiun`), link dummy dihapus. **Admin: MASIH DUMMY 6 dari 7 widget** — `DashboardController@index` hanya memasok EWS; view berisi angka "228", "Ahmad Fauzi", `$cutiList`/`$distribusiGolongan`/`$auditLog` array, SVG tren statis, tombol `alert('Ini adalah data dummy')`; tombol Setuju/Tunda hanya manipulasi innerHTML. Tidak ada `BuildAdminDashboardAction`. Kerjakan sesuai kontrak **K-3** (tanpa tombol keputusan cuti — Admin bukan approver). Sisa gap Pimpinan (minor): W7 masih berbasis `created_at` (kurva step pada data import) dan Non-Aktif/Mutasi tidak dikecualikan historis; fallback `golongan_awal` tampil `- → III/c`; perbaikan #125 belum punya test regresi. **Progres 27 Juli (BE Jordan, belum merge):** gap fallback `golongan_awal` SELESAI. Penyebabnya string `'-'` bersifat truthy sehingga guard `!empty()` di Blade selalu lolos dan cabang penampil golongan tunggal menjadi kode mati; fallback dihapus sehingga nilainya `null`. Blade tidak diubah karena cabangnya sudah benar sejak awal, hanya tidak pernah terjangkau. Ditambah 3 test regresi (payload tanpa pangkat sebelumnya, jalur positif dua riwayat, render golongan tunggal); gate `composer qa` 1.069 lulus / 5.176 assertions di PostgreSQL. **CATATAN KONTRAK UNTUK #16 / dashboard Admin (Grantly):** field `golongan_awal` sekarang bertipe **`?string`** dan `null` berarti pegawai belum punya pangkat sebelumnya. **Jangan menyalin pola lama `?? '-'`** saat membangun `BuildAdminDashboardAction`; penanda teks apa pun akan lolos guard `!empty()` sehingga bug tampilan golongan asal palsu muncul kembali di sisi Admin. Pola yang benar: `$prevRank?->golongan?->kode` tanpa fallback. Bentuk dan daftar key payload tidak berubah sehingga kontrak K-3 tetap aman; yang berubah hanya tipe nilai satu field, dan ini selaras dengan K-3 butir empty state jujur tanpa literal palsu. Field `golongan_tujuan` serta `golongan` sengaja tetap memakai `'-'` karena keduanya nilai tampilan dari FK non-nullable, bukan input yang diuji keberadaannya. Sisa gap #39 lain (W7 basis `created_at`, eksklusi historis Non-Aktif/Mutasi, test regresi #125 milik Grantly) belum tersentuh; W7 menunggu #16 merge sesuai K-3. |
| #40 | Dashboard Pegawai (pribadi) | ⚠️ | **PR #123 menutup** profil ringkas (40.1), card saldo (40.2), daftar cuti aktif (40.3), EWS pribadi (40.4), responsive (40.6). **Sisa:** (a) **BUG — widget 5 notifikasi permanen kosong**: `DashboardController.php:64` memfilter `user_id` dengan `$user->id` padahal FK kolom itu menunjuk `employees` → harus `$user->employee_id` (fix satu baris); (b) fallback literal "12" saat pegawai tanpa baris saldo; (c) tanpa `BuildPegawaiDashboardAction` (query inline, `latestPosition.unitKerja` tidak di-eager-load); (d) tanpa feature test saldo/cuti/notifikasi — penyebab bug (a) lolos. **Progres 27 Juli (BE Jordan, PR #132 — MERGED ke `development`):** (a), (c), (d) selesai — bug FK diperbaiki via `BuildPegawaiDashboardAction` + eager-load `positionHistories.unitKerja` + 6 feature test (TDD, PostgreSQL); kontrak `saldoCuti = null` saat tanpa baris saldo sudah dikunci test. Sisa: (b) empty state FE (Adriel). |
| #41 | Dashboard Kepala Bagian | ⚠️ | **PR #125 menutup:** quick action keputusan 4 tombol + modal catatan wajib langsung di dashboard (reuse endpoint keputusan resmi), default halaman cuti "Menunggu Keputusan" (+escape `all`, dikunci test), pagination bawahan & cuti (whitelist 10/25/50). **Sisa:** (a) per keputusan **K-2**, bersihkan kode "Dinas Luar" setengah jadi yang kini dead code (opsi filter di FilterRequest, cabang action, badge kondisional di detail bawahan) — TIDAK ada implementasi Dinas Luar di Fase 1; (b) EWS bawahan masih array tanpa paginator — kontrol jumlah data/pagination kosmetik; (c) minor: filter "Menunggu Keputusan" berbasis status pengajuan, bukan step-assignee. |
| #42 | Export daftar pegawai (Excel + PDF) | ⚠️ | Excel standar & custom (whitelist kolom aman, anti-formula) tersedia; sisi Pimpinan tuntas via PR #125 (sidebar → route pimpinan, filter custom lengkap, UI read-only). **Sisa (tak tersentuh sejak audit):** (a) **P0 PII** — route preview masih closure `web.php:241` yang mengirim `email_pribadi` + `no_hp` seluruh pegawai (termasuk nonaktif) ke DOM; `ExportPegawaiPreviewAction`/`LaporanController` dead code; scope preview ≠ Excel; test justru mengunci perilaku salah; (b) **PDF pegawai belum ada sama sekali** (42.9–42.14) — hanya `window.print()`; (c) modal custom Admin mengikat 7 state Alpine yang tidak didefinisikan + tanpa tombol pembuka; (d) urutan kolom custom Admin dipaksa canonical (versi Pimpinan sudah mengikuti input user — samakan). |
| #43 | Export rekap cuti (Excel + PDF) | ⚠️ | Excel 2 sheet + PDF dengan filter/role-gate/anti-formula tersedia. **Sisa (file tak tersentuh sejak PR #104):** (a) sheet 2 "Saldo Cuti" berisi bucket saldo, bukan ringkasan per pegawai — implementasi benar sudah ada di jalur Pimpinan (`ExportLeaveReportAction`, sheet "Ringkasan Cuti") → konsolidasikan; (b) nama file `Laporan_Cuti_…` ≠ `Rekap_Cuti_{periode}_{tanggal}.xlsx` (test mengunci nama salah); (c) tabel PDF per pengajuan, bukan rekap per pegawai; (d) footer PDF tanpa nomor halaman (pola benar ada di `ExportRankHistoryPdfAction`). **Progres 27 Juli (BE Jordan, belum merge):** (a) dan (b) SELESAI + agregasi PDF untuk (c) SELESAI. Workbook Excel Admin kini **3 sheet**: `Detail Cuti`, **`Ringkasan Cuti` (baru)** = rekap per pegawai per jenis cuti (hanya status `disetujui`, dikelompokkan `employee_id`+`jenis_cuti_id`, saldo dari `LeaveBalance`, judul kolom saldo mencantumkan tahun), dan `Saldo Cuti` **dipertahankan** (keputusan pemilik task: sheet itu dijaga 4 test dan Issue #43 tidak meminta penghapusannya, jadi nol fitur/test hilang). Nama file `Laporan_Cuti_Ymd_His.xlsx` → **`Rekap_Cuti_{periode}_{Ymd}.xlsx`**. Agregasi ditempatkan di `CutiRekapQuery::summaryRows()` sehingga dipakai bersama Excel + PDF; `ExportCutiPdfAction` kini mengirim `summaryRows` + `periodLabel` ke view. Proteksi anti-formula & freeze pane dipertahankan dan diterapkan juga pada sheet baru. Gate: `composer qa` **1.050 lulus / 5.074 assertions** di PostgreSQL, Pint, PHPStan L3, Vite build. Jalur Pimpinan (`app/Actions/Reports/`) **nol tersentuh** (terverifikasi via `git diff`). **Untuk Adriel:** kontrak data PDF sudah terbit di `kontrak-pdf-rekap-cuti-sprint6.md` (variabel view `$summaryRows`/`$periodLabel`, bentuk baris, contoh payload) — sisa (c) tabel rekap di template dan (d) nomor halaman siap dikerjakan, datanya sudah tersedia. Satu penyelarasan diminta: template baris 30 masih mencetak `$filters['periode']` mentah → sebaiknya `$periodLabel` agar judul, isi, dan nama berkas konsisten. |
| #46 | Kelola reference tables | ⚠️ | **PR #134 MERGED 27 Juli (squash `06190ad`) — Slice 0–2 + UI lima tabel selesai.** **Slice 4 channel notifikasi selesai implementasi lokal 29 Juli dan menunggu commit/PR.** Sisa scope: CRUD `ref_jabatan` (naik prioritas per K-4) dan tab mock non-CRUD. **Slice 3 (`ref_unit_kerja` hierarkis) MERGED ke `development` via PR #147 pada 29 Juli (squash `0b94960`):** CRUD berjenjang lengkap dengan aturan nama unik se-sistem, penolakan penonaktifan unit induk yang masih memiliki sub-unit aktif, dan jenis unit terkunci pada empat nilai resmi (lembaga, bagian, tim kerja, urusan). Penghapusan unit induk yang masih memiliki sub-unit ditolak melalui katalog pemakaian; sebelumnya relasi induk bersifat null-on-delete sehingga sub-unit dapat menjadi unit tertinggi tanpa jejak. Kedalaman unit dihitung otomatis dari induknya dan tidak dapat dikirim dari formulir, serta seluruh sub-unit ikut disesuaikan dalam satu transaksi ketika induknya dipindahkan. Tab unit kerja pada halaman Data Master kini membaca database dengan tampilan berjenjang, menyediakan field `keterangan`, dan pilihan unit induk mengecualikan unit itu sendiri beserta seluruh sub-unitnya. Validasi server menelusuri seluruh rantai induk untuk menolak siklus, rantai korup, dan ancestor nonaktif. Mutasi create/update/toggle/delete berbagi advisory transaction mutex; keunikan nama exact diperiksa ulang setelah lock, audit bersifat fail-closed dalam transaksi, dan cache baru dihapus setelah commit. Verifikasi lokal final: 31 test Unit Kerja / 127 assertions; `composer qa` PostgreSQL 1.114 lulus / 5.378 assertions; Pint 584 berkas; PHPStan 567 berkas tanpa error. CI PR #147 sukses: Pint 596 berkas, PHPStan tanpa error, 1.138 test / 5.555 assertions. Smoke browser delapan skenario tanpa galat konsol dengan jejak audit lengkap tetap menjadi bukti manual pra-merge. **Nasib `ref_bup` SUDAH DIPUTUSKAN per K-4 (27 Juli 2026): di-deprecate, cakupan CRUD issue ini turun dari 9 menjadi 8 tabel.** Kondisi awal sebelum PR (arsip): route `/data-master` closure render view statis; nol controller/FormRequest/Action CRUD untuk 9 reference table; form modal tanpa `name`/`action`/`@csrf` (klaim "RESTRIKSI INTEGRITAS" hanya teks); tidak ada UI kelola `notification_event_channels` (kebijakan PR #122 hanya bisa diubah via DB); model referensi tanpa lifecycle hapus. **Kerjakan sesuai keputusan K-1** (hybrid `is_active`, tanpa SoftDeletes): migrasi `is_active` untuk tabel yang belum punya → CRUD per vertical slice dengan validasi item terpakai → audit → gate `role:super_admin` → UI Data Master data DB + tree unit hierarkis + halaman channel & event notifikasi. **Progres 27 Juli (BE Jordan, branch `feat/kelola-data-referensi`, belum merge):** Slice 0 — migrasi `is_active` 6 tabel (`ref_golongan`, `ref_jenis_jabatan`, `ref_status_pegawai`, `ref_eselon`, `ref_jenjang_pendidikan`, `ref_bup`) + event audit `CONFIG_UPDATE` (constraint diperluas additif) + kontrak field FE diterbitkan. Slice 1 — CRUD `ref_eselon` & `ref_jenjang_pendidikan` (FormRequest, Action generik, controller tipis, guard item terpakai — kritis karena FK eselon `nullOnDelete`, audit lengkap, flush cache dropdown, gate `role:super_admin`, 18 feature test PostgreSQL). ⚠️ Temuan untuk keputusan PM: `ref_bup` tabel yatim — tidak ada FK/pemakaian; kalkulasi BUP nyata memakai `ref_jabatan.default_bup` → `ref_jenis_jabatan.maks_usia_pensiun` → config EWS. CRUD `ref_bup` ditunda sampai diputuskan disambungkan atau di-deprecate. **Slice 2 (27 Juli)** — CRUD `ref_golongan`, `ref_jenis_jabatan`, `ref_status_pegawai` (pola sama Slice 1: FormRequest, Action generik, controller tipis, audit, flush cache `ref.golongan`/`ref.jenis_jabatan`/`ref.jabatan_with_jenis`/`ref.status_pegawai`, gate `role:super_admin`) + **guard baris sistem status pegawai**: kode `AKTIF`/`PENSIUN` dan baris `is_default` tidak dapat dihapus/dinonaktifkan dan kode+nama-nya terkunci di form (dirujuk followup pensiun EWS, fallback status pegawai baru/import, filter EWS via kolom legacy `status_aktif`); `is_default` tidak bisa diubah dari form CRUD; `ref_jenis_jabatan.nama` dijaga unik di validasi (DB tidak punya unique constraint); 29 feature test PostgreSQL baru. **UI Data Master (27 Juli, fullstack):** 5 tab CRUD (golongan, jenis jabatan, eselon, status pegawai, jenjang pendidikan) kini render data database + form nyata (tambah, edit inline, nonaktif/aktifkan, hapus dengan konfirmasi; CSRF; flash sukses; kembali ke tab asal setelah submit) menggantikan array mock; tab baru "Status Pegawai" dengan badge Data sistem/Default (baris terkunci tanpa tombol nonaktif/hapus, kode+nama readonly); kolom jumlah pemakai dihitung agregat (GROUP BY, bukan per baris); route `/data-master` pindah dari closure ke `DataMasterController`; 4 feature test halaman + smoke e2e browser via dev-login lulus (tambah→hapus golongan tercatat audit CREATE/DELETE, nol error console, layout responsif). |

### Arsip update #46 Slice 4 — 29 Juli 2026

**Slice 4 channel notifikasi SELESAI IMPLEMENTASI LOKAL, menunggu commit/PR.** Halaman khusus Super Admin kini mengelola master channel dan kebijakan 13 event dengan desired state eksplisit, status raw/efektif fail-closed, audit atomik, pagination server-side, serta kill-switch email yang diperiksa ulang saat queued job diproses. WhatsApp Business tampil jujur sebagai belum tersedia dan config/credential tidak diekspos. Verifikasi final: focused PostgreSQL **76 test / 353 assertions**; `composer qa` **1.169 test / 5.762 assertions**; Pint **612 berkas**; PHPStan level 3 tanpa error; Vite build dan smoke browser final lulus; reviewer final **Approved**. Issue #46 tetap ⚠️ karena CRUD `ref_jabatan` dan tab referensi lain masih tersisa.

## Urutan Pengerjaan (selaras pembagian tugas 26 Juli)

1. **#40 fix bug notifikasi** (satu baris + test) dan **#42a preview PII** — dampak besar, mulai duluan.
2. **#39 Dashboard Admin** (kontrak K-3) dan **#46** (keputusan K-1) — jalan paralel, FE mulai dari dummy sesuai kontrak.
3. **#42b PDF pegawai + #43 export cuti** — berpasangan (pola action + template PDF, banyak reuse).
4. **#42c/d custom Admin, #41 sisa Kabag** — menyusul.
5. **Kunci regresi perbaikan PR #125** (test `totalEwsAktif`, distribusi granular, golongan asal→tujuan, EWS search, per_page, quick action) + sempurnakan W7 serempak Admin & Pimpinan.

## Riwayat Perubahan Status

| Tanggal | Perubahan |
|---|---|
| 10 Agustus 2026 (rekonsiliasi final source) | Seluruh issue Sprint 6 selesai pada source: #39 melalui PR #150/#158/#164; #40 melalui PR #132/#167; #41 melalui PR #125/#155; #42 melalui PR #154/#162/#167; #43 melalui PR #141/#167; dan #46 melalui PR #151/#165/#170. Status ini tidak menggantikan full regression/UAT Sprint 7. |
| 29 Juli 2026 (#46 Slice 4) | **Slice 4 channel notifikasi selesai implementasi lokal, menunggu commit/PR.** Master channel, matriks kebijakan 13 event, audit fail-closed, pagination, perlindungan config/credential, dan recheck kill-switch queued email selesai. Focused PostgreSQL 76/353, `composer qa` 1.169/5.762, Pint 612 berkas, PHPStan level 3, Vite build, smoke browser final, dan reviewer final seluruhnya lulus. |
| 22 Juli 2026 | Baseline audit (61 task audit: 15 selesai / 22 sebagian / 24 belum; #46 ❌). |
| 23 Juli 2026 | PR #123: dashboard pegawai 40.1/40.2/40.3/40.6 tertutup. |
| 24–26 Juli 2026 | PR #125: quick action + default antrean + pagination Kabag; seluruh perbaikan Pimpinan (#39 sisi Pimpinan, #42 sisi Pimpinan). Ditemukan bug baru FK notifikasi dashboard pegawai (40.5). |
| 26 Juli 2026 | Kickoff selesai: keputusan K-1/K-2/K-3 dikunci; pembagian tugas BE/FE final; status di atas ditetapkan per HEAD `1b2e5b6`. |
| 27 Juli 2026 | BE #40 selesai di branch `fix/dashboard-pegawai-notifikasi` (bug FK notifikasi + `BuildPegawaiDashboardAction` + 6 test). BE #46 Slice 0–1 di branch `feat/kelola-data-referensi` (migrasi `is_active` 6 tabel, event audit `CONFIG_UPDATE`, CRUD eselon + jenjang pendidikan, 21 test; qa penuh 1.005 lulus di PostgreSQL). Kontrak field Data Master untuk FE diterbitkan. Open question PM: nasib `ref_bup` (tabel yatim tanpa pemakai). Keduanya belum merge — status simbol menyusul saat PR masuk. |
| 27 Juli 2026 (lanjutan) | #40 tuntas merge: PR #132 (squash) masuk `development`; sisa porsi FE (b) empty state saldo tetap di Adriel. BE #46 Slice 2 selesai di branch yang sama: CRUD golongan, jenis jabatan, status pegawai + guard baris sistem status (AKTIF/PENSIUN/is_default tidak bisa dihapus/nonaktif/ganti identitas), 29 test baru; qa penuh 1.040 lulus di PostgreSQL. Belum merge — PR gabungan Slice 0–2 menyusul. **Koordinasi FE (Adithian) — penting:** porsi "bongkar data-master" untuk 5 tabel CRUD di atas SUDAH DIKERJAKAN Jordan secara fullstack (termasuk tab baru Status Pegawai) — jangan double work. Porsi FE yang tersisa: tab mock lain (jenis cuti, agama, jenis kelamin, status perkawinan, unit kerja, BUP, hari libur — menunggu slice/keputusan scope-nya), polish UI/UX lanjutan, dan halaman channel notifikasi (Slice 4, kontrak menyusul). |
| 27 Juli 2026 (merge) | **PR #134 merged ke `development` (squash `06190ad`, CI hijau)** — #46 Slice 0–2 + UI Data Master lima tabel tuntas; simbol #46 dinaikkan ke ⚠️ (sisa Slice 3–4 + ref_bup). Jordan lanjut ke #43/#21 (export rekap cuti, P0); Adriel akan menerima kontrak query agregasi PDF dari BE begitu siap. |
| 27 Juli 2026 (#43 BE) | **#43 porsi BE (Excel + query agregasi PDF) SELESAI, menunggu PR/merge.** Sheet `Ringkasan Cuti` per pegawai, nama berkas `Rekap_Cuti_{periode}_{Ymd}.xlsx`, agregasi bersama di `CutiRekapQuery::summaryRows()` dipakai Excel dan PDF. Sheet `Saldo Cuti` sengaja dipertahankan sehingga workbook menjadi 3 sheet dan nol test dihapus. Review menemukan dua hal yang sudah diperbaiki: query detail dijalankan dua kali (kini koleksi dilewatkan), dan pengelompokan memakai nama jenis cuti padahal `ref_jenis_cuti.nama` tidak unik di skema sehingga dua jenis bernama sama bisa menyatu menjadi satu baris dengan total tergabung (kini memakai `jenis_cuti_id`). `composer qa` 1.050 lulus di PostgreSQL. **Kontrak PDF terbit untuk Adriel:** `kontrak-pdf-rekap-cuti-sprint6.md` — porsi template PDF (tabel rekap per pegawai, nomor halaman) kini tidak lagi blocked. Catatan lintas-tim: export massal PII belum ter-audit di seluruh codebase (nol event `EXPORT` pada constraint `audit_logs`); ini pre-existing dan butuh migration tersendiri, dicatat sebagai follow-up di luar scope #43. |
| 27 Juli 2026 (merge #141) | **PR #141 merged ke `development` (squash `f74bb91`, CI hijau)** - #43 porsi BE (Excel + query agregasi PDF) tuntas. Porsi PDF Adriel tidak lagi blocked; kontrak data sudah tersedia. |
| 27 Juli 2026 (#39 fallback golongan) | **Gap fallback `golongan_awal` pada #39 SELESAI, menunggu PR/merge.** Nilai `'-'` diganti `null` sehingga baris kenaikan pangkat pegawai tanpa pangkat sebelumnya berhenti menampilkan transisi golongan palsu; Blade tidak diubah karena cabang penampil golongan tunggal sudah benar dan hanya tidak terjangkau. Tiga test regresi ditambahkan, `composer qa` 1.069 lulus di PostgreSQL. **Untuk Grantly (#16):** `golongan_awal` kini `?string` dengan `null` berarti tanpa pangkat sebelumnya; pakai `$prevRank?->golongan?->kode` tanpa `?? '-'` agar bug yang sama tidak lahir ulang di dashboard Admin. Bentuk key payload tidak berubah sehingga K-3 tetap aman. Sisa #24 yaitu penyempurnaan basis W7 tetap menunggu #16 merge dan keputusan formula dari PM. |
| 28 Juli 2026 (#46 Slice 3) | **Slice 3 kelola unit kerja hierarkis SELESAI, menunggu PR/merge.** Tab unit kerja pada halaman Data Master tidak lagi memakai data contoh; kini membaca database dengan tampilan berjenjang beserta jumlah pemakainya. Aturan yang diberlakukan: nama unit unik se-sistem, penonaktifan unit induk ditolak selama masih ada sub-unit aktif, dan jenis unit terkunci pada empat nilai resmi yaitu lembaga, bagian, tim kerja, dan urusan. Penghapusan unit induk yang masih memiliki sub-unit juga ditolak; ini penting karena relasi induk sebelumnya bersifat null-on-delete sehingga sub-unit bisa berubah menjadi unit tertinggi tanpa jejak. Kedalaman unit dihitung otomatis dari induknya, tidak dapat dikirim melalui formulir, dan seluruh sub-unit ikut disesuaikan dalam satu transaksi ketika induknya dipindahkan. Pilihan unit induk pada formulir mengecualikan unit itu sendiri beserta seluruh sub-unitnya sehingga struktur berputar tidak dapat dibentuk dari antarmuka, dengan validasi server sebagai penjaga terakhir. Verifikasi: 24 test terhadap PostgreSQL, `composer qa` 1.099 lulus / 5.297 assertions, Pint 581 berkas, analisis statis bersih, pembangunan aset berhasil, serta smoke browser delapan skenario meliputi penambahan sub-unit, pemindahan induk beserta penyesuaian kedalaman, penolakan penonaktifan induk, penonaktifan dan penghapusan unit terbawah, serta penolakan nama ganda. Tidak ada galat konsol dan jejak audit tercatat untuk keempat jenis perubahan. Sisa cakupan #46: Slice 4 channel notifikasi dan CRUD `ref_jabatan`. Dua hal dicatat sebagai lanjutan terpisah: nilai bawaan kolom jenis unit di database masih di luar empat nilai resmi sehingga perlu penyesuaian skema, dan unit nonaktif masih tampil pada daftar pilihan penugasan; keduanya konsisten dengan kondisi lima tabel referensi sebelumnya. |
| 29 Juli 2026 (merge #147) | **PR #147 merged ke `development` (squash `0b94960`, CI hijau)** — #46 Slice 3 `ref_unit_kerja` hierarkis tuntas di source. Perbaikan final menambahkan field `keterangan`, validasi seluruh rantai induk, invariant ancestor aktif, advisory mutex mutasi, pemeriksaan ulang nama setelah lock, audit atomik fail-closed, dan invalidasi cache setelah commit. Verifikasi lokal final: 31 test Unit Kerja / 127 assertions; `composer qa` PostgreSQL 1.114 lulus / 5.378 assertions; Pint 584 berkas; PHPStan 567 berkas tanpa error. CI: Pint 596 berkas, PHPStan tanpa error, 1.138 test / 5.555 assertions. #46 tetap ⚠️ karena Slice 4, CRUD `ref_jabatan`, dan tab referensi lain masih tersisa; follow-up schema default `jenis_unit` dan selector unit nonaktif tetap terbuka. |
| 27 Juli 2026 (K-4 ref_bup) | **Open question `ref_bup` ditutup lewat keputusan bernomor K-4.** `ref_bup` di-deprecate: tidak disambungkan ke perhitungan BUP dan tidak dibuatkan CRUD; cakupan CRUD Issue #46 turun dari 9 menjadi **8 tabel**. Sumber BUP resmi Fase 1 adalah `ref_jabatan.default_bup` dengan fallback `ref_jenis_jabatan.maks_usia_pensiun`. Tabel, model, dan seeder tetap dipertahankan pada Fase 1 (migrasi `is_active` sudah menyentuhnya dan sudah berjalan; migrasi destruktif menjelang go-live menambah risiko tanpa manfaat); penghapusan dijadwalkan Fase 2. Dikerjakan sekarang: baris `ref_bup` dicabut dari halaman Pengaturan Sistem, tab statis "Batas Usia Pensiun" pada halaman Data Master dicabut bersama form modalnya (tab itu menampilkan data hardcoded beserta tombol Tambah/Edit/Hapus yang tidak menyimpan apa pun — keberhasilan palsu), model `RefBup` ditandai `@deprecated`, dan pemakaian runtime-nya dilepas dari test sambil mempertahankan assertion kompatibilitas kolom. **Konsekuensi wajib:** CRUD `ref_jabatan` naik prioritas dalam cakupan Slice #46 karena menjadi satu-satunya jalur Admin mengatur BUP per jabatan detail; saat ini belum ada controller, FormRequest, route, tab, maupun test untuknya. **Backlog Sprint 7 P1 baru:** presedensi BUP global versus per jabatan — `EwsEngineService` memperlakukan `pensiun_required_age_years` sebagai override penuh sehingga bila diisi 58 maka pejabat tinggi ikut dihitung 58, bertentangan dengan keputusan meeting PRD §926; urutan yang benar menempatkan global sebagai fallback paling kalah, dan `EwsSchedulerTest` yang saat ini mengunci perilaku override harus diubah lebih dulu. Dokumen yang direvisi: Kickoff (K-4 baru), Issue #46, Issue #11 (ditandai deprecated, tidak dicoret, karena seeder dipertahankan), US-8.5 AC-1, PRD §10/§16.2/§16.14, Role-Super-Admin. |

---

## Addendum Evaluasi LLDIKTI — 31 Agustus 2026

> Rekap **6 ✅ · 0 ⚠️ · 0 ❌** tetap merupakan status source Sprint 6 sebelum kebutuhan baru masuk. [US-8.6 Reporting Statistik](../PRD-DLL/User-Stories-SIMPEG-Fase1.md) belum memiliki issue number, owner, atau Story Point yang disetujui.

| Pekerjaan baru | Definition of Done | Status |
|---|---|---|
| Halaman Reporting Statistik Kepegawaian | Halaman terpisah dari dashboard ringkas, Data Master, dan export; agregat/chart minimal untuk golongan, jenis jabatan, jabatan, unit kerja, dan jenis kepegawaian | Belum Direncanakan |
| Query dan akses | Agregasi database yang scoped/bounded, state kosong, masking/authorization mengikuti dashboard-laporan, serta tidak memuat seluruh data pegawai ke browser | Belum Direncanakan |
| QA | Test data scope dan agregasi, smoke browser desktop/tablet/mobile, responsive layout, serta tidak ada console error | Belum Direncanakan |

Penetapan nomor issue, owner, dan estimasi dilakukan dalam planning berikutnya; tracker ini tidak mengarang assignment atau mengubah evidence implementasi Sprint 6 yang telah tercatat.
